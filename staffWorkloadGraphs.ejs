<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Staff Workload Comparison</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        h1 { text-align: center; margin-bottom: 20px; }
        .chart-container { width: 80%; margin: 0 auto; }
    </style>
</head>
<body>
    <h1>Staff Workload Comparison</h1>

    
    <div class="chart-container">
        <canvas id="workloadChart"></canvas>
    </div>

 
    <div class="chart-container">
        <canvas id="efficiencyChart"></canvas>
    </div>

    <script>
        const workloadData = <%= workloadData %>;

        // Workload Bar Chart
        const ctx1 = document.getElementById('workloadChart').getContext('2d');
        new Chart(ctx1, {
            type: 'bar',
            data: {
                labels: workloadData.states,
                datasets: [
                    {
                        label: 'Workload (Cases)',
                        data: workloadData.workloads,
                        backgroundColor: ['rgba(54, 162, 235, 0.5)', 'rgba(255, 99, 132, 0.5)'],
                        borderColor: ['rgba(54, 162, 235, 1)', 'rgba(255, 99, 132, 1)'],
                        borderWidth: 1
                    },
                    {
                        label: 'Staff Available',
                        data: workloadData.staff,
                        backgroundColor: ['rgba(75, 192, 192, 0.5)', 'rgba(153, 102, 255, 0.5)'],
                        borderColor: ['rgba(75, 192, 192, 1)', 'rgba(153, 102, 255, 1)'],
                        borderWidth: 1
                    }
                ]
            },
            options: {
                responsive: true,
                scales: {
                    y: { beginAtZero: true }
                }
            }
        });

        
        const ctx2 = document.getElementById('efficiencyChart').getContext('2d');
        new Chart(ctx2, {
            type: 'bar',
            data: {
                labels: workloadData.states,
                datasets: [
                    {
                        label: 'Workload Per Staff',
                        data: workloadData.workloadPerStaff,
                        backgroundColor: ['rgba(255, 206, 86, 0.5)', 'rgba(75, 192, 192, 0.5)'],
                        borderColor: ['rgba(255, 206, 86, 1)', 'rgba(75, 192, 192, 1)'],
                        borderWidth: 1
                    }
                ]
            },
            options: {
                responsive: true,
                scales: {
                    y: { beginAtZero: true }
                }
            }
        });
    </script>
</body>
</html> -->
